CREATE TABLE `test`.`products` (
  `id` INT NOT NULL,
  `typesId` INT NULL,
  `name` VARCHAR(45) NULL,
  `price` VARCHAR(45) NULL,
  `quantity` INT NULL,
  `status` VARCHAR(45) NULL,
  PRIMARY KEY (`id`));
